[package]
name = "disk-mosaic-tui"
version = "0.1.0"
edition.workspace = true
authors.workspace = true
license.workspace = true
keywords.workspace = true
description = "The terminal user interface (TUI) for Disk Mosaic, built with `ratatui`."
readme = "README.md"

[dependencies]
crossterm = "0.29.0"
disk-mosaic-core = {
    path = "../disk-mosaic-core",
    version = "0.1.1"
}
humansize.workspace = true
ratatui = "0.30"
treemap = { workspace = true }